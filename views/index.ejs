<!DOCTYPE html>
<html lang="en">
<%- include('include/head.ejs') %>
    <body>
        <%- include('include/header.ejs') %>
            <div class="container p-5 shadow-lg">
                <div class="row">
                    <div class="col-xl-12 col-lg-12 col-md-12 col-sm-12">
                        <p class="lead">API JSON<a href="./api/" class="p-3"><i class="bi bi-box-arrow-up-right"></i></a></p>
                        <article>
                            <% page_content.forEach(page_paragraph => { %>
                                <p><%= page_paragraph %></p>
                            <% }); %>
                        </article>
                    </div>
                </div>
            </div>
    </body>
</html>